{"tablesSkipped":
 [{"table": "link",
   "reason":
   "composite primary key (a, b); LeanDB requires a single INTEGER PRIMARY KEY rowid alias"}],
 "tablesImported":
 [{"table": "customers",
   "struct": "Legacy.Customers",
   "skippedColumns": [],
   "notes": [],
   "columns":
   [{"nullable": false,
     "notes": [],
     "mapping": "newtype CustomersName (raw String, identity validator)",
     "declaredType": "TEXT",
     "column": "name"},
    {"nullable": true,
     "notes": [],
     "mapping": "newtype CustomersEmail (raw String, identity validator)",
     "declaredType": "TEXT",
     "column": "email"},
    {"nullable": false,
     "notes": [],
     "mapping": "Float",
     "declaredType": "REAL",
     "column": "balance"}]},
  {"table": "orders",
   "struct": "Legacy.Orders",
   "skippedColumns": [],
   "notes": [],
   "columns":
   [{"nullable": false,
     "notes":
     ["source FK actions are ON DELETE NO ACTION, ON UPDATE NO ACTION, MATCH NONE; the adopted file keeps them, while a future LeanDB table rebuild normalizes the typed FK to RESTRICT"],
     "mapping": "Ref Customers",
     "declaredType": "INTEGER",
     "column": "customer_id"},
    {"nullable": false,
     "notes": [],
     "mapping": "newtype OrdersItem (raw String, identity validator)",
     "declaredType": "TEXT",
     "column": "item"},
    {"nullable": false,
     "notes": [],
     "mapping": "Int64",
     "declaredType": "INT",
     "column": "qty"}]}],
 "source": "examples/import-fixture/legacy.db",
 "ok": true,
 "notes":
 ["Import loose, tighten forever: every text column is a named newtype with an identity validator — tighten `make` when you know the rule.",
  "On first open the engine creates `_leandb_meta` inside the adopted file and stores the schema fingerprint; this is expected.",
  "The engine's DDL is CREATE TABLE IF NOT EXISTS — a no-op on the adopted tables; existing data is read through the generated codecs."],
 "notCarried":
 [{"tables": ["orders"],
   "reason":
   "views are not imported; it remains in the adopted database file, invisible to the typed layer, until a rebuild drops or rewrites a table it references",
   "name": "big_orders",
   "kind": "view"},
  {"tables": ["orders"],
   "reason":
   "indexes are not represented in the generated schema (no @[index] emission yet); the physical index remains in the adopted database file until a rebuild drops or rewrites its table",
   "name": "idx_orders_customer",
   "kind": "index"},
  {"tables": ["customers"],
   "reason":
   "SQLite default \"0\" is not lifted into the generated field; inserts must supply the field, and a future LeanDB rebuild will not preserve this source default",
   "name": "customers.balance",
   "kind": "default"},
  {"tables": ["orders"],
   "reason":
   "source uses ON DELETE NO ACTION, ON UPDATE NO ACTION, MATCH NONE; the adopted file retains those actions, but LeanDB rebuilds emit RESTRICT",
   "name": "orders.customer_id",
   "kind": "foreign-key action"}],
 "dbPath": "data/legacy.db",
 "base": "legacy"}
